interface:
  display_name: "Matomo Twig Development Rules"
  short_description: "Apply Matomo Twig safety and template guardrails"
  default_prompt: "Use $matomo-twig-development-rules to review or author Matomo Twig template changes, especially raw output handling, escaping, helper usage, template nonce patterns, and whether a core Twig helper used in a plugin template exists in the minimum Matomo the plugin's plugin.json declares."
